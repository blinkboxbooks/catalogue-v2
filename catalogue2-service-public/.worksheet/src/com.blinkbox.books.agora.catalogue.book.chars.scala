package com.blinkbox.books.agora.catalogue.book

object chars {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(115); 

  val ch = List.fill(5)(('a' + 3).toChar).mkString;System.out.println("""ch  : String = """ + $show(ch ));$skip(45); 

  println("Welcome to the Scala worksheet")}
}
